config={_id:"{{ mongod_port }}", members:[{ _id:0, host:"{{ primary_host }}:{{ mongod_port }}", priority:10}]}
rs.initiate(config)
sleep(13000)
{% for host in groups['replication_servers'] %}
rs.add("{{ host }}:{{ mongod_port }}")
sleep(8000)
{% endfor %}
printjson(rs.status())
